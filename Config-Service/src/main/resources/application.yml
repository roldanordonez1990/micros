server:
  port: 8081

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/roldanordonez1990/Microservices-Spring-Cloud-Server.git
          searchPaths: Config-data
          username: ${GIT_USER}
          password: ${GIT_PASSWORD}
          default-label: "master"
