server:
  port: 8081

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/roldanordonez1990/micros.git
          searchPaths: Config-data
          username: ${GIT_USER}
          password: ${GIT_PASSWORD}
          default-label: "v2Hystrix"
